# PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/ai_agent_workshop
PI_WEB_DATA_DIR=./data
PI_WEB_JWT_SECRET=

# AES-256-GCM master key for at-rest secret encryption (MCP configs, API keys).
# 64-char hex = 32 bytes. Generate with: openssl rand -hex 32
APP_ENCRYPTION_KEY=
